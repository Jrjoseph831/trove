{
  "name": "@trove/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Trove server: DynamoDB repository + Lambda handlers (settlement, read, seed). Reuses @trove/engine unchanged.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@trove/data": "*",
    "@trove/engine": "*",
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5",
    "vitest": "^3"
  }
}
