{
  "name": "@trove/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Trove economic engine — pure, DOM-free, runs client-side (sandbox) and server-side (Lambda) unchanged.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trove/data": "*"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
