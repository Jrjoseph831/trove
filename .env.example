# Copy this file to ".env" (same folder) and put your real key after the =.
# .env is gitignored — your key is never committed. Never paste the key in chat.
OPENAI_API_KEY=sk-your-key-here

# Optional overrides for scripts/gen-property-images.mjs:
# MODEL=gpt-image-1      # or dall-e-3
# QUALITY=medium         # gpt-image-1 only: low | medium | high
